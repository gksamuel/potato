<?php

/**
 * This file contains application configuration values
 * such as database credentials
 * other configurations such as log paths could be set here
 */

/**
 * HOST is the name or ip address where your database server is located.
 */
        const HOST = "localhost";

/**
 * USERNAME is the username for database user
 */
        const USERNAME = "andela";

/**
 * PASSWORD is the password for your database user
 */
        const PASSWORD = "@nd3l@";

/**
 * DATABASE is the name of your database instance
 */
        const DATABASE = "andela";
